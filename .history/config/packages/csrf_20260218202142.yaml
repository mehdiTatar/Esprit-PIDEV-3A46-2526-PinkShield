# CSRF Protection Configuration for Symfony 6.4
framework:
    form:
        csrf_protection:
            enabled: true
            field_name: _csrf_token

    # Global CSRF protection settings
    csrf_protection:
        enabled: true
